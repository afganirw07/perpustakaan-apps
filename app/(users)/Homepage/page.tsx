import Homepage from "@/components/Homepage/test";
import { SafeAreaView } from "react-native-safe-area-context";

export default function RegisterPage() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Homepage />
        </SafeAreaView>
    );
}